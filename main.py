from mess_handler import send_message
from version_handler import show_version

show_version()
send_message("📈 Cofure Bot đã kích hoạt — chờ tín hiệu sắp tới!", platform="Messenger")
